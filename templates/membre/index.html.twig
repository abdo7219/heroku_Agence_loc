{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}

<table class="table">

<thead>
<tr>
    <th>id membre</th>
    <th>pseudo</th>
    <th>nom</th>
    <th>prenom</th>
    <th>email</th>
    <th>civilte</th>
    <th>statut</th>
    <th>date_enregistrement</th>
    <th>action</th>
</tr>
</thead>
<tbody>
{% for user in users %}
    <tr>
        <td>{{ user.id }}</td>
        <td>{{ user.pseudo }}</td>
        <td>{{ user.nom }}</td>
        <td>{{ user.prenom }}</td>
        <td>{{ user.email }}</td>
        <td>{{ user.civilte }}</td>
        <td>{{ user.statut }}</td>
        <td>{{ user.dateEnregistrement|date('d/m/Y à H:i:s')}}</td>
        <td><a href="{{ path('blog_show', {id: comment.article.id}) }}">{{ comment.article.title}}</a></td>
    </tr>

{% endfor %}
</body>

</table>

{% endblock %}